# @dictadata/webbrowser-filesystem

A dictadata FileSystem plugin for accessing files from web pages.

A FileSystem plugin for use with @dictadata/storage-junctions v1.2.1+

Browser based file downloads from directory listing enabled web pages. Uses Playwrite as the automation interface to control a Chromium browser instance.

Implements list(), createReadStream() and download() methods.
